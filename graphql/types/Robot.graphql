""" Simple robot. """
type Robot @model(class: "App\\Models\\Robot\\Robot") {
  id: ID!
  name: String!
}

extend type Query {
  robot(
    id: ID! @scope(name: "id")
  ): Robot! @find

  robots: [Robot!]! @paginate(defaultCount: 10)
}
